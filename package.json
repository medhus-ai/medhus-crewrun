{
  "name": "@medhus/router",
  "version": "0.1.0",
  "private": true,
  "description": "Shared agent runtime for Medhus crews: engines, role runner, tool broker, MCP bridge, secret store, runner profiles.",
  "type": "module",
  "exports": {
    "./*": "./src/*.js",
    "./package.json": "./package.json"
  },
  "files": [
    "src",
    "templates",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.170",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@openai/codex-sdk": "^0.139.0",
    "zod": "^4.4.3"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "BUSL-1.1",
  "devDependencies": {
    "better-sqlite3": "^12.10.0"
  }
}
